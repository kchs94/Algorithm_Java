package 프로그래머스.레벨1;
/*
* k만큼 반복하면서 던지는 사람을 찾는 문제
* 원형이므로 n-1 --> 1, n-->2로 던진다라는 조건을 추가한다.
* 자꾸 인덱스와 엣지에서 헷갈린다.
* */
public class 공던지기 {
    /*public int solution(int[] numbers, int k) {
        int target = numbers[0];

        for (int i=1; i<=k; i++) {
            target = 2*i-1;
            if (target == numbers.length-1)
                target =
        }

        return 1;
    }*/
}
