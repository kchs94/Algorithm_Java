package 붓캠.데일리;
/*
* 1.부분 집합 최대합 구하기
* - 데이터 크기:
* - 제한시간
*
* 2.수도코드
* - max = Integer.MINVALUE;
* - 조합을 구한다.
* -
* - if(구한값 < bound)
*   max = Math.max(max, 구한값);
* */
public class SubsetSum_48 {
    public int subsetSum(int[] set, int bound) {
        return 0;
    }
}
